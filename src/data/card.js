// Active card re-export. Seasonal cards live in src/data/cards/ — swap the
// import here (or add a picker) when a new year's card ships.
export { default, WINNING_HANDS, CATEGORIES, CARD_META } from './cards/card2026.js';
